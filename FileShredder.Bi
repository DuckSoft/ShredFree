Declare Sub ShredProc(ByRef filepath As String)

Declare Sub Shred_ZeroAll(filepath As String)
Declare Sub Shred_Pseudo(filepath As String, times As UINT)
Declare Sub Shred_DODC(filepath As String, times As UINT)
Declare Sub Shred_DODE(filepath As String, rounds As UINT)
